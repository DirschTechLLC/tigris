%h1.page-title= t(".title")

.card.max-w-md
  = form_with model: @organization, class: "space-y-4" do |f|
    .space-y-1
      = f.label :name, t(".name"), class: "field-label"
      = f.text_field :name, required: true, autofocus: true, class: "field-input"
      - @organization.errors[:name].each do |msg|
        %p.text-destructive.text-xs.mt-1= msg

    = f.submit t(".submit"), class: "btn-primary"
